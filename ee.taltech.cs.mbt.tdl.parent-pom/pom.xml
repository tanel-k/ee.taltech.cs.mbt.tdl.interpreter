<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ee.taltech.cs.mbt.tdl</groupId>
    <artifactId>parent-bom</artifactId>
    <version>1.5</version>
    <relativePath>../ee.taltech.cs.mbt.tdl.parent-bom/pom.xml</relativePath>
  </parent>

  <artifactId>parent-pom</artifactId>
  <version>1.5</version>
  <packaging>pom</packaging>

  <modules>
    <module>../ee.taltech.cs.mbt.tdl.commons.utils</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.test.test-utils</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.test.sexpr.s-expression-grammar</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.test.sexpr.s-expression-model</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.test.sexpr.s-expression-parser</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.facades.antlr-facade</module>
    <module>../ee.taltech.cs.mbt.tdl.commons.facades.st-facade</module>
    <module>../ee.taltech.cs.mbt.tdl.uppaal.uta-model</module>
    <module>../ee.taltech.cs.mbt.tdl.expression.tdl-model</module>
    <module>../ee.taltech.cs.mbt.tdl.uppaal.uta-grammar</module>
    <module>../ee.taltech.cs.mbt.tdl.expression.tdl-grammar</module>
    <module>../ee.taltech.cs.mbt.tdl.uppaal.uta-parser</module>
    <module>../ee.taltech.cs.mbt.tdl.expression.tdl-parser</module>
    <module>../ee.taltech.cs.mbt.tdl.uppaal.uta-pickler-plugin</module>
    <module>../ee.taltech.cs.mbt.tdl.scenario.scenario-composer</module>
    <module>../ee.taltech.cs.mbt.tdl.user_interface.user-interface-core</module>
    <module>../ee.taltech.cs.mbt.tdl.user_interface.user-interface-cli</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.antlr</groupId>
        <artifactId>antlr4</artifactId>
        <version>${antlr.version}</version>
      </dependency>
      <dependency>
        <groupId>org.antlr</groupId>
        <artifactId>ST4</artifactId>
        <version>${stringTemplate.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-api</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-engine</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-params</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>jaxb2-maven-plugin</artifactId>
          <version>${jaxb2Plugin.version}</version>
          <configuration>
            <clearOutputDir>false</clearOutputDir>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${mavenCompiler.version}</version>
          <configuration>
            <source>${source.version}</source>
            <target>${target.version}</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefirePlugin.version}</version>
          <configuration>
            <useFile>false</useFile>
          </configuration>
        </plugin>
        <plugin>
          <groupId>ee.taltech.cs.mbt.tdl.uppaal</groupId>
          <artifactId>uta-pickler-plugin</artifactId>
          <version>${uta.pickler.version}</version>
        </plugin>
        <plugin>
          <groupId>org.antlr</groupId>
          <artifactId>antlr4-maven-plugin</artifactId>
          <version>${antlr.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${mavenCleaner.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${mavenShadePlugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <source.version>1.8</source.version>
    <target.version>1.8</target.version>
    <antlr.version>4.5</antlr.version>
    <mavenCompiler.version>3.3</mavenCompiler.version>
    <mavenCleaner.version>3.1.0</mavenCleaner.version>
    <stringTemplate.version>4.0.8</stringTemplate.version>
    <surefirePlugin.version>2.22.1</surefirePlugin.version>
    <mavenShadePlugin.version>3.2.0</mavenShadePlugin.version>
    <jaxb2Plugin.version>2.3</jaxb2Plugin.version>
    <junit.version>5.4.2</junit.version>
  </properties>

  <name>TDL(TP) Interpreter Parent POM</name>
  <description />
  <inceptionYear>2019</inceptionYear>
  <organization>
    <name>TalTech</name>
    <url>https://www.taltech.ee</url>
  </organization>
  <developers>
    <developer>
      <id>null</id>
      <name>Tanel Prikk</name>
      <email>tanel.prikk@taltech.ee</email>
      <organization>TalTech</organization>
      <organizationUrl>https://www.taltech.ee</organizationUrl>
      <roles>
        <role>developer</role>
      </roles>
    </developer>
  </developers>

  <issueManagement>
    <system>GitLab</system>
    <url>https://gitlab.cs.ttu.ee/Tanel.Prikk/iapb/issues</url>
  </issueManagement>
</project>
